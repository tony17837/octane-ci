#!/usr/bin/env bash
## Initializes the project
##
## This script is only intended to be run via "fin"

# Initialize stack.
fin project start

# First time project creation, remove old Octane composer lock file.
# Allows project to have its own lock file.
rm -rf composer.lock

# Run the common init script within the cli container.
fin exec bin/init.sh -y

# Install initial site.
fin exec bin/install -y

# Repeat here just to show the site URL when we are done.
fin project start
